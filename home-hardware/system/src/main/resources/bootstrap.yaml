spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: system
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: gaox
        group: home
        enable-remote-sync-config: true
        file-extension: yaml
